// Kept in sync with `@media (max-width: ${COMPACT_LAYOUT_BREAKPOINT_PX}px)` in src/styles.css
// (enforced by src/__tests__/constants.test.ts, since plain CSS can't reference a JS value).
export const COMPACT_LAYOUT_BREAKPOINT_PX = 560;
